#include "main.h"

/**
 * mul - checks for checks for a digit (0 through 9)
 * @a: a - Variable
 * @b: b - Variable
 * Return: always 0
 */

int mul(inta, int b)
{
int mul;

mul = a * b;
return (mul);
}
