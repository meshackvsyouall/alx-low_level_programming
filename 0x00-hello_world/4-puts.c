#include <stdio.h>
/**
 * main -a proogram that uses the puts function to print a line
 * Return: 0 (success)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
